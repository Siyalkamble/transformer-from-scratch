# --- Stage 1: core (attention, RoPE, causal mask test) 
numpy==2.5.1
pytest==9.1.1
torch==2.13.0+cpu

# --- Stage 2: BPE tokenization + TinyStories training + ablations ---
datasets==5.0.0
tokenizers==0.22.2
matplotlib==3.11.0
tqdm==4.68.4

# --- RAG stage ---
sentence-transformers==5.6.0
faiss-cpu==1.14.3

# --- Deployment 
fastapi==0.139.0
uvicorn==0.51.0

# PyTorch CPU wheels
--extra-index-url https://download.pytorch.org/whl/cpu